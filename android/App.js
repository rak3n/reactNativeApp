import React,{useState} from 'react';
import {
  StyleSheet,
  TextInput,
  View,
  Text,
  FlatList,
  Button
} from 'react-native';

var data=[]

const App = () => {
  return (
      <View style={styles.textView}>
        Hekkki
      </View>
  );
};

const styles = StyleSheet.create({
  textView:{
    flex:1
  },
});

export default App;
